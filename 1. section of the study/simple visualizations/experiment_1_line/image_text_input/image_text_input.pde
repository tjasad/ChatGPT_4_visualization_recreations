size(300, 300);  // Set the size of the window
background(255); // Set the background color to white
stroke(0);       // Set the line color to black
line(0, height / 2, width, height / 2);  // Draw the line in the middle of the window
save("line.jpg");
